/* 

パート1（基本機能）
HTML：

エラー数を表示（最初は単純に数字だけでOK）

見つけた文字を表示

選択可能な文字を表示

CSS：

デザインは自由。ただしレスポンシブ対応を意識しましょう。

JavaScript：

最初は1単語を変数に持たせ、それを当てさせる。

単語の文字数をユーザーに表示。

各ボタンはクリックに反応するようにする。

console.log()でボタンから得られた文字を確認する。

ボタンの文字が単語に含まれているかを確認：

含まれていれば、その位置に表示（同じ文字が複数あっても対応）。

含まれていなければ、ミスとしてカウントし、終了に近づいたことを伝える。

単語をすべて当てれば勝利メッセージ。

ミスの上限に達したら敗北メッセージ。

パート2（機能拡張）
単語を1つではなく、複数の単語を配列にしてランダムに選ばせる。

ミスのカウント表示を、棒人間の絵やアニメーションで表現する。

ユーザーがキーボード入力で文字を選べるようにする。

一度使った文字は再度選べないようにする。

勝敗が決まったら、その単語を表示する。

ページをリロードしなくても新しいゲームを開始できるようにする。

パート3（JSON対応）
単語リストを配列ではなく、JSONファイル（自作またはmots.json）から読み込む。

ゲーム終了時に単語を表示し、その単語をクリックするとGoogle検索へ飛べるようにする。

難易度選択を導入。簡単モードでは最初の文字を表示する。

*/