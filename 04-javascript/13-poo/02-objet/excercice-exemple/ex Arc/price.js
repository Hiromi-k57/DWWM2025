"use strict";

const pricegame = {
    form : document.createElement('form'),
    input : form.createElement('input'),
    error : form.createElement('<p>'),
    trial : 0,
    choosedNumber,
    nombreAleatoire : Math.floor(Math.random() * 100),
    coups           : 0,
    nombreChoisi,

    
}